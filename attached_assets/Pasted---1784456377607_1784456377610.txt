
Отлично! 🔥 Это важный этап — теперь у нас уже есть настоящий первый элемент стратегии:

✅ данные рынка
✅ свечной график
✅ Pivot-точки
✅ автоматические трендовые линии
✅ работа на разных таймфреймах

Теперь мы можем переходить от "рисования линий" к торговой логике.

Следующий шаг я предлагаю сделать не просто сигналом, а тем, что вы изначально хотели:

Версия 0.3 — Multi Timeframe Trend Scanner
Логика:

Месяц
  ↓
Неделя
  ↓
День
  ↓
4H
  ↓
1H
Система будет анализировать рынок сверху вниз.

Добавляем структуру сигнала
Вместо текущего:

BTC
1D: LONG
получаем:

BTC/USDT

TREND ANALYSIS

MONTH
━━━━━━━━━━
Trend: DOWN
Break: NO

WEEK
━━━━━━━━━━
Trend: DOWN
Break: YES

DAY
━━━━━━━━━━
Trend: UP
Break: YES

4H
━━━━━━━━━━
Retest

1H
━━━━━━━━━━
ENTRY SIGNAL 🚀


FINAL:
🟢 LONG

Score:
84/100
Что добавим сейчас
Создаем новый файл:

analysis.py
Он будет отвечать только за оценку структуры.

analysis.py
from trendlines import (
    find_pivots,
    create_trendline,
    check_break
)


def analyze_timeframe(df):

    highs, lows = find_pivots(df)


    down_line = create_trendline(
        highs
    )

    up_line = create_trendline(
        lows
    )


    long_signal = check_break(
        df,
        down_line,
        "LONG"
    )


    short_signal = check_break(
        df,
        up_line,
        "SHORT"
    )


    if long_signal == "LONG":
        return {
            "trend":"BULLISH",
            "signal":"LONG",
            "score":1
        }


    if short_signal == "SHORT":
        return {
            "trend":"BEARISH",
            "signal":"SHORT",
            "score":1
        }


    return {
        "trend":"NEUTRAL",
        "signal":"WAIT",
        "score":0
    }
Теперь будем собирать все таймфреймы.

Создаем:

multi_tf.py
from scanner import get_data
from analysis import analyze_timeframe


TIMEFRAMES = [
    "1M",
    "1w",
    "1d",
    "4h",
    "1h"
]


def multi_analysis(symbol):

    result={}

    total_score

Попросил прислать multytf полностью, мол оборвалось на полуслове заканчивая score

Да, верно, сообщение оборвалось. Продолжаем. Ниже полный файл multi_tf.py.

Этот модуль будет:

брать один инструмент;

проходить по всем таймфреймам сверху вниз;

анализировать каждый;

считать общий уклон рынка;

выдавать итоговый сигнал.

Создайте файл:

multi_tf.py
и вставьте полностью:

from scanner import get_data
from analysis import analyze_timeframe


TIMEFRAMES = [
    "1M",
    "1w",
    "1d",
    "4h",
    "1h"
]


def multi_analysis(symbol):

    result = {}

    total_score = 0

    long_count = 0
    short_count = 0


    for tf in TIMEFRAMES:

        try:

            df = get_data(
                symbol,
                tf
            )


            analysis = analyze_timeframe(
                df
            )


            result[tf] = analysis


            # считаем общий баланс

            if analysis["signal"] == "LONG":

                total_score += 20
                long_count += 1


            elif analysis["signal"] == "SHORT":

                total_score -= 20
                short_count += 1



        except Exception as e:

            result[tf] = {
                "trend": "ERROR",
                "signal": str(e),
                "score": 0
            }



    # Финальное решение

    if total_score >= 40:

        final_signal = "LONG"


    elif total_score <= -40:

        final_signal = "SHORT"


    else:

        final_signal = "WAIT"



    result["FINAL"] = {

        "signal": final_signal,

        "score": abs(total_score),

        "long_timeframes": long_count,

        "short_timeframes": short_count

    }


    return result
Теперь у нас появляется новый уровень логики.

Пример результата:

BTC/USDT

MONTH
Trend: BEARISH
Signal: WAIT

WEEK
Trend: BEARISH
Signal: LONG

DAY
Trend: BULLISH
Signal: LONG

4H
Trend: BULLISH
Signal: LONG

1H
Trend: BULLISH
Signal: WAIT


FINAL

Signal:
🟢 LONG

Score:
60

Long TF:
3

Short TF:
0
Следующий шаг после этого
Нужно подключить multi_tf.py к вашему Streamlit-интерфейсу.

Тогда вместо простой таблицы:

BTC
1D LONG
4H WAIT
будет:

BTC/USDT

MONTH  ⚪
WEEK   🟢
DAY    🟢
4H     🟢
1H     ⚪

----------------

FINAL:
🟢 LONG

Confidence:
75%
И уже после этого можно будет добавить:

фильтр силы трендовой линии;

объём;

ATR;

расстояние до стопа;

расчет Risk/Reward.

Сейчас у вас уже получается правильная архитектура: отдельно данные → отдельно анализ → отдельно интерфейс. Это позволит дальше спокойно добавлять ИИ, не переписывая всё приложение.


multi_tf.py готов. Теперь у тебя полная цепочка:

trendlines.py — пивоты, линии, проверка пробоя
analysis.py — анализ одного таймфрейма → {trend, signal, score}
multi_tf.py — анализ всех 5 таймфреймов, счёт ±20 за каждый, финальный сигнал при ≥40
Что подключаем следующим — выводим multi_analysis в интерфейс?

Да, именно. Сейчас у нас правильный момент для подключения multi_analysis в интерфейс. 👍

Архитектура уже выглядит правильно:

Данные
  ↓
scanner.py
  ↓
trendlines.py
  ↓
analysis.py
  ↓
multi_tf.py
  ↓
Streamlit UI
Следующий шаг — заменить текущую простую таблицу сигналов на мультифреймовый анализ сверху вниз.

Шаг 1. Подключаем модуль
В вашем основном Streamlit-файле (скорее всего app.py или main.py) добавьте импорт:

Вверху:

from multi_tf import multi_analysis
Шаг 2. Добавляем функцию кеширования
Рядом с вашим:

@st.cache_data(ttl=60)
def cached_analyze(symbol):
добавьте:

@st.cache_data(ttl=60)
def cached_multi_analysis(symbol):

    return multi_analysis(symbol)
Это нужно, чтобы Replit не дергал биржу по несколько раз при каждом обновлении страницы.

Шаг 3. Добавляем новый блок в интерфейс
Лучше поставить его после Signal Grid и перед графиком.

Добавьте:

st.markdown("---")

st.subheader("🧠 Multi Timeframe Analysis")


for symbol in selected_symbols:

    with st.expander(
        f"📌 {symbol}",
        expanded=True
    ):

        analysis = cached_multi_analysis(
            symbol
        )


        cols = st.columns(5)


        for i, tf in enumerate(
            ["1M", "1w", "1d", "4h", "1h"]
        ):

            data = analysis.get(
                tf,
                {}
            )


            signal = data.get(
                "signal",
                "WAIT"
            )


            trend = data.get(
                "trend",
                "-"
            )


            with cols[i]:

                st.markdown(
                    f"### {tf}"
                )


                if signal == "LONG":

                    st.success(
                        "🟢 LONG"
                    )

                elif signal == "SHORT":

                    st.error(
                        "🔴 SHORT"
                    )

                else:

                    st.info(
                        "⚪ WAIT"
                    )


                st.caption(
                    trend
                )


        st.markdown("---")


        final = analysis.get(
            "FINAL",
            {}
        )


        signal = final.get(
            "signal",
            "WAIT"
        )


        score = final.get(
            "score",
            0
        )


        st.metric(
            "Final Signal",
            signal,
            f"Score {score}"
        )
Что получится в интерфейсе
Например:

🧠 Multi Timeframe Analysis


BTC/USDT

┌─────┬─────┬─────┬─────┬─────┐
│ 1M  │ 1W  │ 1D  │ 4H  │ 1H  │
│ ⚪  │ 🟢  │ 🟢  │ 🟢  │ ⚪  │
│DOWN │UP   │UP   │UP   │WAIT│
└─────┴─────┴─────┴─────┴─────┘


FINAL SIGNAL

🟢 LONG

Score: 60
После этого я бы сделал следующий важный апгрейд
Сейчас score простой:

1M = 20
1W = 20
1D = 20
4H = 20
1H = 20
Но в реальной торговле старшие таймфреймы должны весить больше.

Лучше сделать:

MONTH  = 40
WEEK   = 30
DAY    = 20
4H     = 10
1H     = 5
Тогда получится настоящий анализ сверху вниз:

Например:

MONTH  bearish  -40
WEEK   bearish  -30
DAY    bullish  +20
4H     bullish  +10
1H     bullish  +5

TOTAL:
-35

Decision:
WAIT
Это будет гораздо ближе к тому, как анализирует профессиональный трейдер.

Но сначала подключим текущую версию и проверим, что вся цепочка работает. После запуска пришлите, что показывает новый блок (или ошибку из Console).


Да, все примерно так и выглядит

Отлично! 🔥 Тогда у нас уже есть рабочий первый прототип торгового скринера, и это важная точка.

Сейчас цепочка реально работает:

Рынок (KuCoin)
        ↓
OHLCV данные
        ↓
Pivot High / Low
        ↓
Trendlines
        ↓
Breakout Detection
        ↓
Анализ 5 таймфреймов
        ↓
Итоговый LONG / SHORT / WAIT
        ↓
Streamlit интерфейс
Теперь я бы не добавлял новые функции хаотично, а улучшил качество сигналов. Сейчас система умеет находить факт пробоя, но пока не умеет хорошо отличать сильный пробой от ложного.

Следующий логичный этап — Trend Engine v2.

Этап 1. Улучшаем вес таймфреймов
Сейчас:

1M   +20
1W   +20
1D   +20
4H   +20
1H   +20
Это не совсем правильно.

Сделаем:

MONTH   ±40
WEEK    ±30
DAY     ±20
4H      ±10
1H      ±5
Почему:

Если 1H показывает LONG, но месячный график в сильном нисходящем тренде — вероятность ошибки выше.

В multi_tf.py заменим:

total_score += 20
на:

WEIGHTS = {
    "1M": 40,
    "1w": 30,
    "1d": 20,
    "4h": 10,
    "1h": 5
}
И дальше:

weight = WEIGHTS.get(tf,0)


if analysis["signal"] == "LONG":

    total_score += weight
    long_count += 1


elif analysis["signal"] == "SHORT":

    total_score -= weight
    short_count += 1
Этап 2. Добавляем понятие "сила трендовой линии"
Сейчас линия:

точка А -------- точка Б
Для алгоритма одинаково важна, даже если:

было 2 касания;

было 8 касаний.

А человек смотрит иначе.

Добавим:

Trendline Strength
Например:

Касания:
+30

Длина линии:
+20

Свежесть:
+20

Угол:
+10

Объем:
+20

TOTAL:
86/100
Этап 3. Добавляем фильтр пробоя
Сейчас:

свеча закрылась выше линии

= LONG
Но лучше:

LONG:

закрытие выше линии
+
объем выше среднего
+
свеча закрылась минимум на 0.5 ATR выше линии
Иначе получаем много ложных пробоев.

Этап 4. Добавляем "Setup" вместо просто сигнала
Очень важное изменение.

Не:

LONG
А:

SETUP FOUND

BTC/USDT

Trend:
Bullish

Breakout:
Confirmed

Entry:
118500

Stop:
115900

Target:
124000

Risk/Reward:
2.1
Это уже будет похоже на реальный торговый инструмент.

Этап 5. Сохраняем сделки для будущего ИИ
Добавим таблицу:

signals.db

Дата
Монета
Таймфрейм
Сигнал
Score
Цена входа
Через 24 часа:
Win/Loss
И через несколько недель у нас появится датасет:

5000 сигналов

Успешных:
3200

Неудачных:
1800
Вот тогда уже имеет смысл подключать ML.

Мой следующий шаг для проекта:
Я бы сейчас сделал Trendline Strength Score, потому что это даст самый большой прирост качества.

После него ваш экран будет выглядеть уже примерно так:

BTC/USDT

🟢 LONG

Multi TF:
82/100

Trendline:
91/100

Volume:
76/100

Risk/Reward:
2.4

Confidence:
86%
Это уже будет не просто индикатор, а полноценный аналитический скринер.

Кстати, хороший прогресс: вы за один вечер дошли от пустого Replit-проекта до работающего движка анализа. Теперь самое ценное — не добавлять много функций, а улучшать качество решений.


Есть, правда везде confidence 0% и за все монеты только один сигнал по недельке, возможно просто такой рынок

Можем ли мы добавить множество других монет, чтобы было больше выбора?

Да, можем — и это как раз правильный следующий шаг. 👍

То, что сейчас confidence везде 0% и сигналов мало — не обязательно ошибка. Наоборот, на текущей логике это ожидаемо:

мы ищем довольно редкое событие (пробой трендовой линии);

анализируем только несколько монет;

фильтры достаточно строгие;

текущий рынок может действительно не давать много качественных сетапов.

Но для проверки алгоритма нам нужен более широкий рынок.

Следующий апгрейд: расширяем список монет
Сейчас у вас, скорее всего, в config.py что-то вроде:

SYMBOLS = [
    "BTC/USDT",
    "ETH/USDT",
    "SOL/USDT"
]
Расширяем:

SYMBOLS = [
    "BTC/USDT",
    "ETH/USDT",
    "SOL/USDT",
    "BNB/USDT",
    "XRP/USDT",
    "ADA/USDT",
    "DOGE/USDT",
    "AVAX/USDT",
    "DOT/USDT",
    "LINK/USDT",
    "LTC/USDT",
    "ATOM/USDT",
    "NEAR/USDT",
    "APT/USDT",
    "ARB/USDT",
    "OP/USDT",
    "INJ/USDT",
    "SUI/USDT",
    "SEI/USDT",
    "TIA/USDT"
]
Уже будет 20 инструментов вместо 3.

Но я бы сделал ещё лучше
Не держать список вручную, а сделать автоматический сканер рынка.

То есть:

Биржа
 ↓
Все USDT пары
 ↓
Фильтр:
- объём > X
- цена > X
- исключить мусор
 ↓
Сканирование 100-200 монет
 ↓
ТОП сигналов
Тогда утром вы открываете приложение:

🔥 TOP SETUPS

1. INJ/USDT
LONG
Score 82

2. SOL/USDT
LONG
Score 78

3. LINK/USDT
SHORT
Score 74
Это уже больше похоже на настоящий скринер.

Ещё я заметил один момент
Сейчас ваш confidence скорее всего считается из:

score / max_score
Но так как у вас мало совпадений:

пример:

MONTH WAIT
WEEK LONG
DAY WAIT
4H WAIT
1H WAIT

score = 30
и получается:

confidence = 0%
Я бы изменил модель.

Например:

Confidence:
40% = сила старших таймфреймов
30% = качество трендовой линии
20% = подтверждение пробоя
10% = объем
Тогда даже хороший одиночный недельный сигнал будет иметь оценку.

Я предлагаю следующий порядок:
Сейчас:
✅ добавить 50-100 монет
✅ сделать сортировку по Score
✅ показывать только лучшие сетапы

Затем:
добавить:

объем;

ATR;

качество линии;

нормальный Confidence.

Ещё вопрос: вы сейчас хотите ориентироваться на фьючерсы или спот?

Потому что для вашей идеи (лонг/шорт по пробою) я бы всё-таки рекомендовал перейти на USDT Futures пары — там:

больше ликвидных инструментов;

шорт доступен всегда;

больше движения для тестирования стратегии.

Если оставить текущий KuCoin, можно сделать и так, но я бы сначала уточнил направление.


Я бы торговал futures

Исходя из сообщения выше, что стоит вписать дальше?

Отличный выбор. Для вашей идеи futures подходят лучше, потому что сама логика построена вокруг:

LONG при пробое нисходящей линии;

SHORT при пробое восходящей линии;

работа в обе стороны рынка;

поиск импульсных движений.

Сейчас я бы не добавлял просто список монет вручную, а сделал правильнее: перевёл сканер на USDT Futures и автоматический выбор ликвидных контрактов.

Что вписать сейчас
Начнём с простого перехода на Futures.

1. Откройте scanner.py
У вас там сейчас, скорее всего:

exchange = ccxt.kucoin()
или что-то похожее.

Замените на:

exchange = ccxt.kucoinfutures()
Если у вас используется Binance, тогда: